package conf

// Database tests are intentionally minimal since the database is used
// through the public LoadFromDatabase() and SaveToDatabase() methods
// which are integration-tested through the API tests.